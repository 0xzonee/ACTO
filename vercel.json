{
  "version": 2,
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ],
  "installCommand": "mv pyproject.toml pyproject.toml.bak && pip install -r requirements.txt && mv pyproject.toml.bak pyproject.toml"
}

